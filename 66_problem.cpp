#include <iostream>
using namespace std;

// Dr.Mostafa Saad Course Homework

int main()
{
    int a, big ;
    
    cin >> a;
    big = a; cin >> a;
    if (big < a) big = a;cin >> a;
    if (big < a) big = a; cin >> a;
    if (big < a) big = a; cin >> a;
    if (big < a) big = a; cin >> a;
    if (big < a) big = a; cin >> a;
    if (big < a) big = a; cin >> a;
    if (big < a) big = a; cin >> a;
    if (big < a) big = a; cin >> a;
    if (big < a) big = a;

    cout << big << endl;

    return 0;
}
