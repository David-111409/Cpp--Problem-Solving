#include <iostream>
using namespace std;

// Dr.Mostafa Saad Course Homework

int main()
{
    float n, m, k, l, y;
    cin >> n >> m >> k >> l >> y;
  
    cout << (n + m + k + l + y) / 5.0 << endl;
  
    cout << (n + m + k) / (l + y) << endl;
  
    cout << ((n + m + k) / 3 ) / ((l + y) / 2) ;
    return 0;
}
